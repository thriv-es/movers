import { describe, it, expect } from 'vitest'

// pnpm --filter @workspace/tw-preset-shadcn test test/stub.test.ts
describe('@workspace/tw-preset-shadcn test stub', () => {
  it('stub test always passes', () => {
    expect(true).toBe(true)
  })
})
