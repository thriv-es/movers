import { describe, it, expect } from 'vitest'

// pnpm --filter @workspace/tw-preset-workspace test test/stub.test.ts
describe('@workspace/tw-preset-workspace test stub', () => {
  it('stub test always passes', () => {
    expect(true).toBe(true)
  })
})
