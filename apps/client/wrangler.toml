# Cloudflare Pages project configuration
# Matches Git-connected project: movers-front
name = "movers-front"
compatibility_date = "2023-12-18"
pages_build_output_dir = "dist"

[vars]
ENV = "production"
# TODO: Replace with your actual backend Worker URL
BACKEND_URL = "https://movers.assaf-6f5.workers.dev"

[placement]
mode = "smart"

[env.preview]
compatibility_date = "2023-12-18"
[env.preview.vars]
ENV = "preview"

[env.production]
vars = { ENV = "production" }
